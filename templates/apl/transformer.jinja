import builtins

from BLEIR import (
%{- for kind in imports }%
    ${ kind }$,
%{- endfor }%
    SyntacticError,
)

class BLEIRTransformer:%{ for transform_fn in definitions }%

${ transform_fn -}$
%{ endfor -}%
## if definitions|length == 0:
    pass
## endif
